# Session Recap — 2026-04-16 (session 2)

**Project:** academic-works-harvest
**Session focus:** Identify records not covered by the four combined Academic Works sets (etds, pubs, oers, arch) and prepare a report for the Academic Works coordinator.

## What we built / changed
- Saved `src/find_missing_records.py` — standalone script to find records in school-level sets missing from the combined sets (etds, pubs, oers). Previously written inline twice; now reusable with `--sets` flag.
- Cross-referenced the OAI-PMH "Contents of This Site" page, our harvested files, and the Alma import profile dropdown (279 sets). Confirmed all three sources mostly agree on what sets exist.
- Identified **2,420 orphaned records** across 28 sets that don't appear in any of the four combined sets (etds, pubs, oers, arch). Biggest gaps: CUNY Law Review (572), Women's Studies Quarterly (551), The Reporter/Baruch (402), LACUNY Meeting Minutes (332), Urban Library Journal (168), CLAGS (151).
- Generated `data/orphaned_records_for_jojo.xlsx` — two-sheet Excel report (summary by collection + all 2,420 records with titles) for the Academic Works coordinator meeting.

## Key decisions
- **The four combined sets are etds, pubs, oers, and arch** — confirmed by looking at the per-school pages on Academic Works (e.g., Baruch shows Archives, OERs, Pubs, and ETDs as its four collections).
- **Two-profile harvest strategy depends on Alma deduplication** — Option A (etds as qdc + All Sets as oai_dc) is simplest but harvests ETDs twice. Option B (four profiles, one per combined set) avoids overlap but loses the 2,420 orphaned records. Meeting with Jojo tomorrow should clarify whether the orphans can be fixed on the Academic Works side.
- **dc:source mapping**: tried dcterms:relation in Primo VE, didn't work well. Considering "Uniform Title" instead — to be discussed with Academic Works coordinator.

## Files touched
- `src/find_missing_records.py` — new script (~115 lines)
- `data/orphaned_records_for_jojo.xlsx` — new report for Academic Works coordinator
- `docs/session-notes/2026-04-16.md` — recap for earlier work (Hunter LibGuides, dc:source)

## How to run / verify
```bash
cd academic-works-harvest
source venv/bin/activate

# Find missing records (all three sets or specific ones)
python3 src/find_missing_records.py
python3 src/find_missing_records.py --sets etds pubs
```

## Gotchas & things to remember
- **10 sets in the Alma dropdown that we didn't harvest** — cm_pubs, mhc_etds, gc_nano_theses, mtrj, hml, wcib_pubs, bm_studentpubs, lg_arch_sgo, arch_img, cm. Probably empty at harvest time.
- **3 sets we harvested that aren't on the Academic Works front end** — bc_arch_alfred, bc_arch_farm_all, bc_arch_history. Back end exposes them via OAI but they don't show on the website.
- **Journals (clr, wsq, ulj) are the biggest orphan category** — 1,292 records in three journals not assigned to any combined set. These are probably "pubs" but Jojo would need to confirm.
- **The "Suggested Group" column in the Excel report is a guess** — based on collection names, not authoritative. Jojo should review and correct.

## Open questions / next steps
- [ ] Meet with Jojo (Academic Works coordinator) — review the 2,420 orphaned records and ask which combined set each belongs in
- [ ] Ask Jojo about the 32 ETDs missing from the combined etds set (28 Hunter, 3 SPS, 1 Brooklyn College)
- [ ] Test whether Alma deduplicates when the same OAI identifier comes in from two import profiles (this is an Ex Libris question, not a Jojo question)
- [ ] Decide on dc:source mapping — Uniform Title or something else
- [ ] After Jojo meeting: finalize the two-profile vs four-profile harvest strategy
